FROM golang:1.20 AS builder
WORKDIR /app
COPY..
RUN go build -o main

FROM alpine:latest
RUN apk add --no-cache ca-certificates
COPY --from=builder /app/main /app/
WORKDIR /app
EXPOSE 50051
CMD ["/app/main"]